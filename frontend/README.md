# MangaVault Frontend

This directory is reserved for the React client.

Planned source structure:

```text
src/
|-- assets/
|-- components/
|-- contexts/
|-- hooks/
|-- layouts/
|-- pages/
|-- routes/
|-- services/
`-- utils/
```

The React toolchain will be initialized when frontend implementation begins. API calls must be centralized in `src/services`.
