# Vite only loads .env files located in this directory (frontend/), not the repo root.
# Copy to `.env` for local development, or set the variable in the hosting dashboard.
VITE_API_BASE_URL=http://localhost:8080/api
